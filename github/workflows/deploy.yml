on:
  push:
    branches: ["main"]
  pull_requests:
    branches: ["main"]

    jobs: 
      build:
        runs-on:ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - name: List directories 
          run: ls -la
